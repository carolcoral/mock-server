# 版本变更说明

## v2.0.5 (2026-06-12)

> 系统统计、安全修复与体验优化。

### 🚀 新增
- **系统统计页面**：ECharts 可视化图表，请求频率（按天/按小时）、来源 IP TOP15、项目/接口新增趋势、IOPS 实时监控，数据缩放拖拽，仅管理员可访问
- **复制按钮**：接口路径旁新增独立复制图标按钮，hover 行时显现

### 🐛 修复
- **统计页面刷新 401**：JWT 过滤器 SPA 路由白名单遗漏 `/statistics`
- **统计页面刷新 403**：WebConfig 视图控制器遗漏 `/statistics` 的 `index.html` 回退映射
- **统计接口 SQL 报错**：SQLite 不支持 `DATE_FORMAT()`，替换为 `strftime()`
- **端口变更不生效**：run.sh 引入 PID 文件机制，修改 `.env` 端口后正确终止旧进程
- **项目创建者无权限查看接口**：`getMockApisByProjectId` 新增成员权限校验
- **自定义代码编译失败**：DynamicCompiler 兼容 JDK 21+ 类加载器，动态扫描 Maven 全部依赖，修复 `com.alibaba.fastjson` 等第三方库导入失败
- **Chrome 140 复制失败**：新增 `document.execCommand('copy')` 降级兼容方案
- **密码表单标签溢出**：Profile 页和侧边栏修改密码弹窗宽度适配多语言
- **用户头像获取**：基于邮箱 SHA256 哈希生成 Cravatar 头像 URL，加载失败回退默认头像

### ⚡ 优化
- 首页统计数字超 9999 自动显示 `9999+`，hover 显示精确数值
- 统计菜单排序调整至系统设置上方

---

## v2.0.4 (2026-06-12)

> UI/UX 深度美化与配置增强。

### ✨ 新增
- **自定义链接 SVG 图标**：页脚自定义链接支持设置 SVG 图标，实时预览，无图标时回退默认样式
- **页脚可选配置**：版权、友情链接、博客、GitHub、邮箱、自定义链接均支持独立开关，全部关闭时自动隐藏页脚区域

### 🎨 UI 美化
- **登录页**：标题渐变流动动画 + 六边形盒子 SVG 图标，移除底部版权信息
- **侧边栏**：深色渐变背景 + Canvas 动态游走发光线条 + 顶部氛围光晕
- **侧边栏菜单**：hover 左侧光柱滑入 + 渐变高亮 + 图标缩放动效，选中态发光边框
- **侧边栏 Logo**：彩色渐变文字 + 流动动画 + 六边形图标

### 🐛 修复
- 页脚设置保存后页面不自动刷新同步
- `setup-env.sh` 默认执行安装，移除交互确认提示

### ⚡ 优化
- 侧边栏动态线条透明度调低，菜单文字更突出

---

## v2.0.3 (2026-06-11)

> 安全漏洞修复，`npm audit` 清零。

### 🔒 依赖升级
- **axios** `1.13.6` → `1.15.1`（3 个 CVE：HTTP 劫持、RCE、空字节编码）
- **postcss** `8.5.6` → `8.5.10`（XSS 漏洞）
- **vite** `5.x` → `6.x`，配套升级 vitest、@vitejs/plugin-vue
- **Spring Boot** `3.2.0` → `3.2.12`
- 间接依赖（lodash、picomatch 等）同步修复，`npm audit` 零漏洞

### 🔒 Docker 安全加固
- 基础镜像升级，非 root 用户运行
- 容器只读文件系统、禁止提权、最小权限
- npm 构建阶段禁止脚本执行

---

## v2.0.2 (2026-06-11)

> 新功能与核心问题修复。

### 🚀 新增
- **登录页 Bing 每日图片**：随机获取 Bing 每日推荐图片作为背景，2 秒超时自动回退默认图
- **Docker 完整部署方案**：多阶段构建 Dockerfile、docker-compose 编排、一键构建推送脚本

### 🐛 修复
- **Mock 随机返回**：修复条件匹配优先级导致的随机功能失效，权重和随机选择恢复正常
- **CORS / 静态资源 401**：修复生产环境下 Bing 图片跨域和静态资源（图片、字体、JS/CSS）认证拦截问题
- **Favicon**：修复不存在的 vite.svg 引用

---

## v2.0.1 (2026-06-11)

> UI/UX 全面优化。

### ✨ 新增
- **航空主题 UI**：亮色风格 + 航空元素的现代化视觉重设计（首页统计样式不变）
- **操作下拉菜单**：项目管理操作按钮改为下拉菜单，界面更整洁
- **删除安全强化**：全站删除操作统一红色标识 + 危险级别确认对话框

### 🐛 修复
- **系统信息国际化**：修复 16 个字段 i18n 显示异常，三语言同步补齐
- **版本号 key 冲突**：修复 `version` 与 `systemVersion` 键名重复

---

## v2.0.0 (2026-06-10)

> 基于 v1.0.2 的重大版本升级。

### 🚀 新增
- **多语言国际化**：中文简体、English、日本語三语言支持，全站实时切换
- **自定义响应处理器**：Java/JavaScript 动态代码编译器，保存即生效，内置字段脱敏等插件
- **系统监控**：CPU、内存、磁盘实时监控，JVM 堆详情，颜色指示器
- **系统公告**：Markdown 公告编辑，优先级分级，首页展示
- **日期格式配置**：YYYY-MM-DD / DD/MM/YYYY / MM/DD/YYYY 可选，全局统一

### ✨ 增强
- **安全**：登录失败锁定、IP 白名单、JWT 过期可配
- **用户管理**：管理员完整编辑用户信息，权限分级
- **接口管理**：请求参数管理（PATH/QUERY/BODY/FILE），代码处理器集成
- **系统配置**：Axios 超时、配置持久化、统一配置接口

### 🐛 修复
- 语言切换不全站生效、i18n key 显示异常、日期格式不持久化
- 日语环境按钮错位、创建时间换行、401 页面刷新拦截
- 管理员编辑用户无权限、修改密码不退出、个人信息刷新问题

---

## v1.0.2

> 上一稳定版本，包含基础 Mock 功能。

- 前后端分离（Vue 3 + Spring Boot）
- HTTP/WebSocket Mock，多状态码、条件响应、随机响应
- 多项目管理，用户权限控制，JWT 认证
- SQLite + Caffeine 缓存，Docker 部署支持

---

## v1.0.1

> 功能完善与 Bug 修复。

---

## v1.0.0

> 首个正式发布版本。
